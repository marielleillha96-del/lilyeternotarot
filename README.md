# lilyeternotarot

Painel e landing page do Eterno Tarot.

## Variáveis de ambiente

Copie `.env.example` para `.env.local` e configure:

- `NEXT_PUBLIC_SITE_URL`
- `DATABASE_URL`
- `DIRECT_URL`
- `ADMIN_JWT_SECRET`

## Desenvolvimento

```bash
npm install
npm run dev
```
